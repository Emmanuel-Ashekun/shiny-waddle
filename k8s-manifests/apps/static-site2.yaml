apiVersion: v1
    kind: ConfigMap
    metadata:
      name: static-site2-content
      namespace: default
    data:
      index.html: |
        <html><head><title>Static Site 2</title></head>
        <body style="font-family:sans-serif;">
        <h1>Static Site 2</h1>
        <p>This is a demo app for edge deployment.</p>
        <p><strong>Version:</strong> v1.0.0</p>
        </body></html>
    ---
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: static-site2
      labels: { app: static-site2 }
    spec:
      replicas: 1
      selector:
        matchLabels: { app: static-site2 }
      template:
        metadata:
          labels: { app: static-site2 }
        spec:
          # Optional: pin to a specific edge node by label (e.g., site=edge1). Remove if not needed.
          # nodeSelector:
          #   site: edge2
          containers:
            - name: nginx
              image: nginx:1.25-alpine
              ports:
                - containerPort: 80
              volumeMounts:
                - name: web
                  mountPath: /usr/share/nginx/html/index.html
                  subPath: index.html
          volumes:
            - name: web
              configMap:
                name: static-site2-content
    ---
    apiVersion: v1
    kind: Service
    metadata:
      name: static-site2
    spec:
      type: NodePort
      selector: { app: static-site2 }
      ports:
        - port: 80
          targetPort: 80
          nodePort: 30002